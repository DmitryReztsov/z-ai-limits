# Project Rules

## Git Workflow

- NEVER push directly to main. Always create a branch and open a pull request.
- Use `gh pr create` to create PRs. Never push commits directly to main.
- Branch naming: `(feat|fix)/description` — enforced by CI.
